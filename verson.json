{
  "buildCommand": "",
  "outputDirectory": ".",
  "framework": null
}
```

Just create this as `vercel.json` in the same folder as your `index.html` (make sure you rename your file from `margolis-intake-system.html` to `index.html`).

Your repo should look like:
```
/
├── index.html
└── vercel.json
